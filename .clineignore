# Build output
target/
dist/

# Generated at build time by build.rs — read the sources in buildables/ and
# sprites/ instead, and DEVELOPING.md for how they're produced.
assets/generated/

# Binary mesh/lib assets there's nothing useful to read in. The hand-modeled
# Wings3D sources (buildables/*.wings) and their exported .glb are binary too.
*.glb
*.gltf
*.wings
*.ttf
*.png

# Neural-net weights and training data
assets/static/models/
training-log.txt
fake_training/

# Temporary files from Wings3D
buildables/*~
buildables/#*#

.vscode/
LICENSE.md
license.txt
